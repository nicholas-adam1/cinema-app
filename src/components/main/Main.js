import React from 'react';
import MainContent from '../content/MainContent';

const Main = (props) => {
  return (
    <div className="main">
      <MainContent />
    </div>
  );
};

export default Main;
